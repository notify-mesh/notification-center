import { ComingSoon } from "../_chrome/page-shell";

export default function SecurityPage() {
  return (
    <ComingSoon
      title="Security"
      description="2FA, recent auth events, and dangerous actions."
      note="Live security panel is on the roadmap."
    />
  );
}
