import { ComingSoon } from "../_chrome/page-shell";

export default function ProjectsPage() {
  return (
    <ComingSoon
      title="Projects"
      description="Per-tenant containers for templates, notifications, and API keys."
      note="The full Projects workspace lands after API Keys."
    />
  );
}
