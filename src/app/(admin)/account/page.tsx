import { ComingSoon } from "../_chrome/page-shell";

export default function AccountPage() {
  return (
    <ComingSoon
      title="Account"
      description="Profile, locale, timezone, and 2FA settings."
      note="Full profile editor is on the roadmap."
    />
  );
}
