import { ComingSoon } from "../_chrome/page-shell";

export default function NotificationsPage() {
  return (
    <ComingSoon
      title="Notification preferences"
      description="Personal alerts about platform events."
      note="Preferences UI is on the roadmap."
    />
  );
}
