import { ComingSoon } from "../_chrome/page-shell";

export default function SendPage() {
  return (
    <ComingSoon
      title="Send"
      description="Compose and dispatch a notification ad-hoc."
      note="Schedule and send UI is on the roadmap."
    />
  );
}
